# get-powershell-aliases-table
There is an alias for that... Retrieves PowerShell aliases from the local computer and presents the info in various table formatted formats (a Windows PowerShell script).
